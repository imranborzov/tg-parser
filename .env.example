# Optional. If set, these seed the FIRST instance's Telegram credentials on
# first run. You can otherwise leave this file out entirely and enter each
# instance's credentials in the web UI instead.
API_ID=your_api_id_here
API_HASH=your_api_hash_here
PHONE_NUMBER=+1234567890
